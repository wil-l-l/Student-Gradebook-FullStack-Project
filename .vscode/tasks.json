{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Create Component",
      "type": "shell",
      "command": "New-Item -ItemType Directory -Path src/components/${input:name} -Force; New-Item -ItemType File -Path src/components/${input:name}/${input:name}.jsx; New-Item -ItemType File -Path src/components/${input:name}/${input:name}.css",
      "problemMatcher": []
    }
  ],
  "inputs": [
    {
      "id": "name",
      "type": "promptString",
      "description": "Component name (PascalCase)"
    }
  ]
}
